템플릿 메서드 패턴(Template Method Pattern)은 이런 문제를 해결하는 디자인 패턴이다.
템플릿 메서드 패턴은 SubClassLogic1, SubclassLogic2처럼 클래스를 계속 만들어야하는 단점이 있다.
=> 익명 내부 클래스를 사용하면 이런 단점을 보완할 수 있다.
    => 익명 내부 클래스를 사용하면 객체 인스턴스를 생성하면서 동시에 생성할 클래스를 상속받는 자식 클래스를 정의할 수 있다.
        => 이 클래스는 SubClassLogic1 처럼 직접 지정하는 이름이 없고 클래스 내부에 선언되는 클래스여서 익명 내부 클래스라 한다.